<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jackting.module_im">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/msg_ic_launcher"
        android:label="@string/msg_app_name"
        android:roundIcon="@mipmap/msg_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/msg_AppTheme">
        <activity android:name="com.jackting.module_msg.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>