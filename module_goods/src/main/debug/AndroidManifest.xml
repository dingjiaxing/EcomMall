<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jackting.module_goods">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/goods_ic_launcher"
        android:label="@string/goods_app_name"
        android:roundIcon="@mipmap/goods_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/goods_AppTheme"
        android:name="com.jackting.common.CommonApplication"
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>